[project]
name = "data-consumer"
version = "0.1.0"
description = "MQTT → InfluxDB consumer for Virtual Boiler"
authors = [{ name = "You" }]
requires-python = ">=3.12"
dependencies = [
    "paho-mqtt>=2.1.0",
    "influxdb-client>=1.48.0",
    "pydantic-settings>=2.7.0"
]
